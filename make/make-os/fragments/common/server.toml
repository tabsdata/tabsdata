#
# Copyright 2025 Tabs Data Inc.
#

[tasks.__os__ps_pytest_tabsdata_servers]
plugin = "td-task-tracker"
category = "04 - Test"
private = true
script_runner = "@duckscript"
script = [
    '''
    !include_files ./make/make-os/libraries/log.ds

    debug "🚀 python ${PS_SUPERVISORS_SCRIPT}"
    exec --fail-on-error python ${PS_SUPERVISORS_SCRIPT}
    ''']

[tasks.__os__start_pytest_tabsdata_servers]
plugin = "td-task-tracker"
category = "04 - Test"
private = true
script_runner = "@duckscript"
script = [
    '''
    !include_files ./make/make-os/libraries/log.ds

    debug "🔷 _____ Starting tabsdata testing servers..."

    if is_empty ${profile}
        build-folder = set "debug"
    else
        is_dev = eq ${profile} "dev"
        if ${is_dev}
            build-folder = set "debug"
        else
            build-folder = set ${profile}
        end
    end
    if not is_empty ${td-target}
        build-folder = set ${td-target}/${build-folder}
    end
    debug "🔅 Using build folder ${build-folder}"
    os_family = os_family
    if eq ${os_family} "windows"
        tdserver = set "tdserver.exe"
    else
        tdserver = set "tdserver"
    end

    exists = is_path_exists ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver}
    if ${exists}
        debug "🔅 Using tdserver binary: '${os_family}' - '${tdserver}'"

        instances = exec --fail-on-error python ${TESTING_INSTANCES_SCRIPT} ${TESTING_SUPERVISORS_CONFIG}
        instances = split ${instances.stdout} "\n"

        for instance in ${instances}
            instance = trim ${instance}
            if not is_empty ${instance}
                info "🔅 Starting instance: ${instance}"

                instance_arguments = array
                array_push ${instance_arguments} ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver}
                array_push ${instance_arguments} start
                array_push ${instance_arguments} --instance
                array_push ${instance_arguments} ${instance}
                array_push ${instance_arguments} --

                debug "🚀 python ${TESTING_INSTANCE_SCRIPT} ${TESTING_SUPERVISORS_CONFIG} ${instance}"
                instance_config = exec --fail-on-error python ${TESTING_INSTANCE_SCRIPT} ${TESTING_SUPERVISORS_CONFIG} ${instance}
                instance_config = trim ${instance_config.stdout}
                debug "🔅 Output of yaml parser for ${instance}:\n ${instance_config}"

                instance_parameters = split ${instance_config.stdout} "\n"
                for yaml_line in ${instance_parameters}
                    yaml_line = trim ${yaml_line}
                    if not is_empty ${yaml_line}
                        array_push ${instance_arguments} ${yaml_line}
                    end
                end

                command = array_join ${instance_arguments} " "
                debug "🚀 ${command}"

                on_ga = get_env TD_ON_GA
                on_ga_true = eq ${on_ga} "true"
                os = os_family
                if eq ${os} "windows"
                    exec --fail-on-error %{command}
                else
                    if ${on_ga_true}
                        exec --fail-on-error %{command}
                    else
                        output = exec --fail-on-error %{command}
                    end
                end
            end
        end
    else
        warn "🚚 Binaries for tdserver not available. Skipping tabsdata start."
    end

    cm_run_task __os__ps_pytest_tabsdata_servers
    ''']

[tasks.__os__status_pytest_tabsdata_servers]
plugin = "td-task-tracker"
category = "04 - Test"
private = true
script_runner = "@duckscript"
script = [
    '''
    !include_files ./make/make-os/libraries/log.ds

    debug "🔷 _____ Checking tabsdata testing servers..."

    if is_empty ${profile}
        build-folder = set "debug"
    else
        is_dev = eq ${profile} "dev"
        if ${is_dev}
            build-folder = set "debug"
        else
            build-folder = set ${profile}
        end
    end
    if not is_empty ${td-target}
        build-folder = set ${td-target}/${build-folder}
    end
    debug "🔅 Using build folder ${build-folder}"
    os_family = os_family
    if eq ${os_family} "windows"
        tdserver = set "tdserver.exe"
    else
        tdserver = set "tdserver"
    end

    exists = is_path_exists ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver}
    if ${exists}
        debug "🔅 Using tdserver binary: '${os_family}' - '${tdserver}'"

        instances = exec --fail-on-error python ${TESTING_INSTANCES_SCRIPT} ${TESTING_SUPERVISORS_CONFIG}
        instances = split ${instances.stdout} "\n"

        for instance in ${instances}
            instance = trim ${instance}
            if not is_empty ${instance}
                info "🔅 Checking instance: ${instance}"

                debug "🚀 ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver} status --instance ${instance}"

                status_timeout = calc 3 * 60
                status_sleep = set 1000
                status_attempts = calc ${status_timeout} * 1000
                status_attempts = calc ${status_attempts} / ${status_sleep}

                status_attempt = set 0
                status_checking = set true

                while ${status_checking}
                    output = exec --fail-on-error ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver} status --instance ${instance}

                    apiserver_started = contains ${output.stdout} "apiserver"
                    aiagent_started = contains ${output.stdout} "aiagent"
                    janitor_started = contains ${output.stdout} "janitor"
                    if ${apiserver_started} and ${aiagent_started} and ${janitor_started}
                        debug "🔅 Verified supervisor & (apiserver, aiagent, janitor) are running"
                        status_checking = set false
                    else
                        debug ". ${status_attempt}"
                        status_attempt = calc ${status_attempt} + 1
                        status_exhausted = eq ${status_attempt} ${status_attempts}
                        if ${status_exhausted}
                            error "😡 Cancelling execution as supervisor did not start after ${status_timeout} seconds"

                            debug "🛞👀 Supervisor status >>>>>"
                            debug "🛞👀 <out.i>"
                            debug "${output.stdout}"
                            debug "🛞👀 <out.f>"
                            debug "🛞👀 <err.i>"
                            debug "${output.stderr}"
                            debug "🛞👀 <err.f>"
                            debug "🛞👀 <code.i>"
                            debug "${output.code}"
                            debug "🛞👀 <code.f>"
                            debug "🛞👀 <<<<<"

                            exit 1
                        end
                        sleep ${status_sleep}
                    end
                end
            end
        end
    else
        warn "🚚 Binaries for tdserver not available. Skipping tabsdata status."
    end

    cm_run_task __os__ps_pytest_tabsdata_servers
    ''']

[tasks.__os__stop_pytest_tabsdata_servers]
plugin = "td-task-tracker"
category = "04 - Test"
private = true
script_runner = "@duckscript"
script = [
    '''
    !include_files ./make/make-os/libraries/log.ds

    debug "🔷 _____ Stopping tabsdata testing servers..."

    if is_empty ${profile}
        build-folder = set "debug"
    else
        is_dev = eq ${profile} "dev"
        if ${is_dev}
            build-folder = set "debug"
        else
            build-folder = set ${profile}
        end
    end
    if not is_empty ${td-target}
        build-folder = set ${td-target}/${build-folder}
    end
    debug "🔅 Using build folder ${build-folder}"
    os_family = os_family
    if eq ${os_family} "windows"
        tdserver = set "tdserver.exe"
    else
        tdserver = set "tdserver"
    end

    exists = is_path_exists ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver}
    if ${exists}
        debug "🔅 Using tdserver binary: '${os_family}' - '${tdserver}'"

        instances = exec --fail-on-error python ${TESTING_INSTANCES_SCRIPT} ${TESTING_SUPERVISORS_CONFIG}
        instances = split ${instances.stdout} "\n"

        for instance in ${instances}
            instance = trim ${instance}
            if not is_empty ${instance}
                info "🔅 Stopping instance: ${instance}"

                debug "🚀 ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver} stop --instance ${instance}"

                on_ga = get_env TD_ON_GA
                on_ga_true = eq ${on_ga} "true"

                if ${on_ga_true}
                    exec --fail-on-error ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver} stop --instance ${instance}
                else
                    output = exec --fail-on-error ${PROJECT_TABSDATA_ROOT_FOLDER}/target/${build-folder}/${tdserver} stop --instance ${instance}
                end
            end
        end
    else
        warn "🚚 Binaries for tdserver not available. Skipping tabsdata status."
    end

    cm_run_task __os__ps_pytest_tabsdata_servers
    ''']

[tasks.__os__drop_pytest_tabsdata_servers]
plugin = "td-task-tracker"
category = "04 - Test"
private = true
script_runner = "@duckscript"
script = [
    '''
    !include_files ./make/make-os/libraries/log.ds

    debug "🔷 _____ Deleteing tabsdata testing servers..."

    home_folder = set ${HOME}

    instances = exec --fail-on-error python ${TESTING_INSTANCES_SCRIPT} ${TESTING_SUPERVISORS_CONFIG}
    instances = split ${instances.stdout} "\n"

    for instance in ${instances}
        instance = trim ${instance}
        if not is_empty ${instance}
            info "🔅 Droping instance: ${instance}"

            instance_folder = set ${home_folder}/.tabsdata/instances/${instance}
            debug "✅ The tabsdata instance ${instance} folder is: ${instance_folder}"
            exists_instance_folder = is_path_exists ${instance_folder}
            if eq ${exists_instance_folder} true
                debug "✅ The tabsdata instance ${instance} folder exists. Deleting it: ${target_dir}..."
                rm -r ${instance_folder}
                debug "✅ The tabsdata instance ${instance} has been deleted: ${target_dir}!"
            else
                debug "✅ The tabsdata instance ${instance} folder does not exist. Skipping deleting it: ${target_dir}."
            end
        end
    end
    ''']
